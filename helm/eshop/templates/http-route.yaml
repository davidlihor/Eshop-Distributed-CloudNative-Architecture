{{- range .Values.services }}
{{- if and .ports (hasSuffix "-clusterip" .name) (ne .name "yarp-api-gateway-clusterip") }}
{{- $serviceName := .name }}
{{- $host := "" }}
{{- $path := "" }}
{{- $isSpecial := false }}
{{- if eq $serviceName "web-client-clusterip" }}
  {{- $host = $.Values.security.istio.hosts.hostApp }}
  {{- $path = "/" }}
  {{- $isSpecial = true }}
{{- else if eq $serviceName "keycloak-clusterip" }}
  {{- $host = $.Values.security.istio.hosts.hostId }}
  {{- $path = "/" }}
  {{- $isSpecial = true }}
{{- else }}
  {{- $host = $.Values.security.istio.hosts.hostApi }}
  {{- $trimmedName := $serviceName | replace "-clusterip" "" }}
  {{- $path = printf "/%s-service/" $trimmedName }}
{{- end }}

{{- $httpPort := 0 }}
{{- range .ports }}
  {{- if eq .name "http" }}
    {{- $httpPort = .port }}
  {{- end }}
{{- end }}

{{- if $httpPort }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $serviceName }}-http-route
  namespace: {{ $.Values.global.namespace }}
spec:
  parentRefs:
  - name: eshop-gateway
    namespace: istio-system
  hostnames:
  - {{ $host | quote }}
  rules:
  - matches:
    - path: 
        type: PathPrefix
        value: {{ $path }}
    {{- if not $isSpecial }}
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          type: ReplacePrefixMatch
          replacePrefixMatch: /
    - type: CORS
      cors:
        allowOrigins:
        - "https://{{ $.Values.security.istio.hosts.hostApp }}"
        allowMethods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
        allowHeaders:
        - Content-Type
        - Authorization
        - X-Requested-With
        allowCredentials: true
        maxAge: 86400
    {{- end }}
    backendRefs:
    {{- if eq (default "" .strategyType) "canary" }}
    - name: {{ $serviceName }}
      port: {{ $httpPort }}
      weight: 100
    - name: {{ $serviceName }}-canary
      port: {{ $httpPort }}
      weight: 0
      filters:
      - type: RequestHeaderModifier
        requestHeaderModifier:
          add:
          - name: x-version
            value: canary
    {{- else }}
    - name: {{ $serviceName }}
      port: {{ $httpPort }}
      weight: 100
    {{- end }}
{{- end }}
{{- end }}
{{- end }}
